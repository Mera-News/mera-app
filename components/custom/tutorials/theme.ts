// Shared visual constants for the tutorials module. One place so the five
// placeholder kinds read as one designed set rather than five sketches.

/** primary-400 — the app's accent, and the colour any future animation must use. */
export const TUTORIAL_ACCENT = 'rgb(231, 138, 83)';
/** The same accent at card/fill strength. */
export const TUTORIAL_ACCENT_SOFT = 'rgba(231, 138, 83, 0.14)';
export const TUTORIAL_ACCENT_EDGE = 'rgba(231, 138, 83, 0.35)';

export const TUTORIAL_MUTED = 'rgba(255, 255, 255, 0.08)';
export const TUTORIAL_MUTED_EDGE = 'rgba(255, 255, 255, 0.14)';
export const TUTORIAL_TEXT_DIM = 'rgb(163, 163, 163)';

/** Height of the scene block at the top of every slide. */
export const SCENE_HEIGHT = 200;

/** One breath. Every placeholder that pulses uses this so they stay in sympathy. */
export const BREATH_MS = 2600;
