/** Timing for the Tabs selection indicator. Kept in its own module (as upstream
 *  ships it) so a call site can reason about the animation length without
 *  importing the component. */
export const tabsAnimationConfig = {
    indicatorDuration: 200,
};
